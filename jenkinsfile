pipeline {
    agent any

    stages {
        stage("Build Imagens") {
            steps {
                script {
                    dockerapp = docker.build("metuzalemoliveira/api-produto:", "-f ./src/Dockerfile ./src") 
                }
            }
        }
    }
}
