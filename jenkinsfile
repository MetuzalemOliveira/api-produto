pipeline {
    agent any

    stages {
        stage("Build Imagens") {
            steps {
                script {
                    dockerapp = docker.build("fabricioveronez/api-produto", "-f ./src/Dockerfile ./src") 
                }
            }
        }
    }
}
