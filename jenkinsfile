pipeline {
    agent any

    stages {
        stage("Build Imagens") {
            steps {
                script {
                    dockerapp = docker.build("metuzalem/api-produto:", "-f ./src/Dockerfile ./src") 
                }
            }
        }
    }
}
